on:
  push:
    branches:
      - staging

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yaml
    with:
      environment: staging
    secrets: inherit
  deploy:
    needs: test
    name: Deploy
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: staging
    secrets: inherit
